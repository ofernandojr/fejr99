# Configuração de deploy no Netlify
# Site estático: publica a raiz do repositório.

[build]
  publish = "."
  command = ""

# Redireciona qualquer rota para o app (SPA de página única)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
